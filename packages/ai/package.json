{
  "name": "@horizon/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI orchestration layer with provider abstraction, rate limiting, and prompt versioning for Horizon",
  "dependencies": {
    "ai": "^5.0.0",
    "@ai-sdk/openai": "^1.0.0",
    "@ai-sdk/google": "^2.0.0",
    "zod": "^3.22.4",
    "@horizon/config": "workspace:*",
    "@horizon/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest"
  }
}
